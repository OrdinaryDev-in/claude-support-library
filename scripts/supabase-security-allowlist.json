[
  {
    "lint": "authenticated_security_definer_function_executable",
    "function": "is_admin",
    "reason": "is_admin(uuid) must stay EXECUTE-able by `authenticated` so RLS policies can call it on behalf of signed-in users. `anon` EXECUTE was revoked in 0005_revoke_is_admin_anon_execute.sql. Accepted residual risk: any signed-in user can call rpc/is_admin(uid) directly and learn whether an arbitrary uid is an admin — no other data is exposed.",
    "added": "2026-08-15"
  },
  {
    "lint": "auth_leaked_password_protection",
    "entity": "Auth",
    "reason": "Requires the Supabase Pro plan or above (org 'Mubashir-Mohamed's Org' is on Free) — there's no dashboard toggle to enable it at this tier. Revisit and remove this entry if/when the org upgrades. Partially mitigated by the app's own minLength=8 password requirement (components/auth/AuthForm.tsx).",
    "added": "2026-08-15"
  }
]
